from machine import Pin, ADC
JohnButton = Pin(16, Pin.IN, Pin.PULL_DOWN)
JaneButton = Pin(28, Pin.IN, Pin.PULL_DOWN)
PotentialMan = ADC(Pin(22))
def Menu():
    if JohnButton
def Song():
    pass
while True:
    Menu()
    Song()